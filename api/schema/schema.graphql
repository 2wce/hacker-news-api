type User {
  email: String!
  id: ID!
  name: String
  posts: [Post!]!
}

type Post {
  by: String
  title: String
  text: String
  score: String
}

type Query {
  me: User
  feed: [Post!]!
  post(storyId: String!): Post
}
